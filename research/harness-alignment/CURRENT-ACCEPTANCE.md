# Current goal acceptance

This is an incomplete-work audit, not a declaration of completion. The original goal is Bonsai 2 27B inside a persistent, conversational generative UI harness for real desktop data, with source review, Semiotic-based views, execution evidence, and reviewed example curation, and MLflow comparisons of model and harness configurations. The user confirmed this first-version scope on September 21, 2026. Weight fine-tuning is outside this version.

The adjacent PROTOTYPE-COMPARISON.md preserves the existing Horthy/Trivedy comparison from frozen bonsai-generative-ui commit a69afd8290de71a18755f934516a0ed9f649afe6. Its implementation status is historical. Current implementation evidence follows.

| Requirement | Current evidence | What it does not yet prove |
| --- | --- | --- |
| Pinned Bonsai 2 27B and image support | Native runtime identity, projector hash, shared queue; vision-runtime and visual-source reports | General visual reasoning quality |
| Desktop files and multiple sources | Immutable originals, collection snapshots, table/text/PDF/Word/email/image/audio/video adapters | Every desktop format, fetch intake, full-document coverage |
| Interpret data before generating UI | Persisted structured proposals, source-quote validation, exact-version confirmation | Correctness of inferred relationships; the user PDF remains unreviewed |
| Collaboration on interpretation | Proposal revision and source correction events | Authenticated team identity, real multi-person review, reviewed proposal-to-training targets |
| Semiotic interactive UI | Source-bound graph/scatter/line plans, generated project scaffold, drilldown and annotation checks | Full v0 parity, arbitrary complex visualization families, reliable end-to-end outcomes across sources |
| Reuse and edit working projects | Persistent revisions, context checkpoints, bounded patch loop and failed-attempt traces | Robust model edit quality; profile trials had unresolved Svelte/interaction failures |
| Media-to-view examples | Vision and speech extraction execute locally with provenance | Native audio reasoning, natural-video understanding, broader audio/image/video-to-view quality beyond the development fixtures |
| Trace-informed harness research | Preserved comparison and MLflow failures tied to context, schema, validation, and loop interventions | A causal improvement estimate from reused development examples |
| Reviewed examples and configuration comparisons | Human-declared extraction review exports exclude tests/Codex/unreviewed records; MLflow records configuration trials and failures | Curated examples supplied by real reviewers and controlled configuration comparisons across a representative versioned evaluation set |
| Recoverable evidence | Last verified full archive 2026-09-21T181036287671+0000 | Newer audio/vision checkpoints: MongoDB upload failed; those remain local/MLflow artifacts |

Development Word, audio, video and mixed-source examples have now reached generated or repaired views, as recorded below. Those demonstrations do not establish general reliability. The next decisive evidence is a controlled comparison on a frozen set of source questions with identical starting code, input context and checks, plus actual reviewer judgments of interpretation and UI usefulness. Tests and development confirmations cannot provide those human judgments. The real PDF proposal must remain untouched until its actual review.

Current remaining work is broader than UI polish. Conversation before attachment now executes with Bonsai and passes its saved context into file planning. Persisted conversations can be reopened from Workstreams without browser draft storage. These are local workstreams, not authenticated team accounts. Source formats and media coverage remain bounded, and model-authored acceptance plans and richer behavior checks remain limited. Reviewed-example publication exists, but real human labels and controlled comparisons across a representative versioned evaluation set are still missing. More chart controls alone cannot close these gaps. The entries below are chronological evidence, so later entries may close a gap stated in an earlier entry.

A curated example must link the original source hash, question, reviewed interpretation, accepted or corrected result, reviewer identity and origin, and relevant MLflow run and trace. Development confirmations stay separate from human review. Configuration comparisons must use the same versioned examples and record model/runtime identity, prompt and harness revision, generation settings, extraction and source-fidelity checks, interaction checks, latency, and loop failures. Reused development examples cannot establish held-out quality.

The latest development repair attempt, 6f35397b7bdb49d987b97b9c6a832ea5, failed after repeating an unmatched patch twice on the unchanged revision. MLflow run de95add866e740f88ebf931de57380f9 preserves the trace. The loop guard stopped the attempt; the mobile layout failure remains unresolved in that saved project.

The maintained starter now passes the source-aware desktop and 390px mobile browser checks on the same three structured video records. The verification used an isolated development project, not the failed saved project. MLflow run 883c8361c55e4849ae5d4db9f0710165 stores the checks and code. Unicode source text is now sent without ASCII escaping in tool results and context checkpoints; ten worker tests pass. Whether this change improves Bonsai patch success remains unproven until a fresh model attempt.

A fresh model attempt after loading commit 99f6765 completed the saved development project. Attempt 88c91b6ce0ac40df91408d7d48dfc4dd applied one patch, passed build and source-aware desktop/mobile browser checks, and saved revision 5199fca9c5dd793d6cf7c67e6d43c7d0beefb44ca17296082f10c02123daf8f9. MLflow run ade474061d6a477aaeb934833ea44beb records 69.59 seconds and zero repairs. The request and starting revision were unchanged from failed run de95add866e740f88ebf931de57380f9. This is one development retry with conversation history; it is not a controlled causal estimate or held-out evaluation. The original failed run remains preserved.

Reviewed extraction exports now include a deterministic dataset SHA-256, task name, and candidate count. Six review tests verify that repeated exports retain the same version, corrected human-declared targets change it, and excluded test feedback does not enter the dataset. Proposal and UI example curation and a user-facing configuration comparison remain outstanding.

Interface curation now has append-only judgments tied to the current saved revision. Edits make earlier acceptance ineligible for the current export. Only the latest human-declared acceptance exports a candidate; rejected, test, Codex, and unreviewed outcomes remain excluded. Exported bundles include source fixtures, saved files, attempt references, review history, and a deterministic dataset hash. MLflow records the bundle and available attempt summaries. Reviewer identity remains self-declared. The Evidence tab now has the corresponding form and export control, verified at desktop and mobile sizes. The browser test saved only explicitly labeled test feedback and confirmed that the MLflow export contained zero human-reviewed examples.

Current integration evidence: Word development job 4fcf01b7f98f4b39b6d78f19bdc330fc completed as an editable RecordTable project, 520028eb62b246b4891abc86e11c84e0. Its confirmation is explicitly development-only. The earlier video source job ad7ee2568c5047858094f3c50c4d4ced remains failed, with a separately traced successful repair of its saved project. The real PDF job 2cd051414c0b40839f6a5753e6adb44a remains awaiting confirmation with no project. These states were checked directly rather than inferred from reports.

Synthetic audio job 40989ae26fc54305973adb5407fd3684 completed proposal, development-only confirmation, table build, and source-aware browser checks. Planning run c8a77704475546d3b92c0b6ee110765c and build run 54ad5cfaa80b4869863d12af492f20f9 preserve the evidence. Project d81cf07e0f48463d9fa4018773d89f73 contains two qualitative requirement records with segment timestamps. This validates the synthetic fixture path, not natural meeting transcription quality. Visual review found excessive columns and machine-style labels; schema selection and presentation remain open.

Incremental audio-project edit 1c09b5124dd84c79b45b957fa2257902 completed in 42.86 seconds with zero repairs. Bonsai added original-source links from the authored evidence map and stopped the Review button from breaking across lines. Source values, search, note persistence, source links, and mobile layout passed. MLflow run f6266b9268044a36ab2fb01b1331cff1 records the effective initial model-input hash. Saved revision 01ef966d61a1c083fe5847c84f1db3bab4666905a90f007937f370b1e8c7c4d9 belongs to project a38c58f2fa114f029c5c8ee8f03a356b. This is development evidence, not a human acceptance or a broad reliability estimate.

Mixed Word and synthetic audio job 60df4f14c15746fd9a34b2232c9abc8c completed as project e13a4a3d2c214a688339afcd92ddb5fd. Planning run 4994b57a5f9548cebfd9e23474b093d4 preserves an initial inconsistent-field schema failure and one successful repair. Build run ce9cee9caf01439e95d50f8c47e8ad25 passed source and interaction checks. The three records keep the document count separate from audio requirements, preserve exact quotes and timestamps, and disclose unread embedded media. Confirmation was explicitly by Codex for development verification. Visual inspection found awkward machine-style labels and excessive raw JSON in record details. These remain design failures despite passing workflow checks. The full Python suite passed 189 tests. The real PDF still awaits confirmation. MIXED-SOURCE-VERIFICATION.json records this evidence and its limits.

Readable record details now replace expanded JSON in the maintained starter; exact JSON remains under Raw data. A separate Bonsai edit of mixed-source project e13a4a3d2c214a688339afcd92ddb5fd added readable fields and selection but needed a second request to widen the layout. Runs 1a9a2bac76d04a3b98609595c8a7f620 and de5f7664a38f451e834547bca1cc0640 preserve both attempts. Saved revision 0392ce27b2b84d49537b8577588ad20d03ce404ff60ffc1e963acbba752fd710 passed source checks and two additional desktop/mobile checks for exact visible values, disclosure, selection and statement width. This exposes a harness gap: baseline verification alone did not prove the initial request was complete. Remaining visual issues include wrapped machine values and accumulated automated notes. READABLE-RECORDS-VERIFICATION.json records the checks and limits.

The edit API now accepts fixed caller-supplied browser request checks. They run at desktop and mobile widths after baseline checks, return failures to the repair loop, and gate completion. The model cannot modify the contract through its tools. The contract hash participates in MLflow task matching. Attempts without a contract explicitly record request verification as not assessed, and the UI distinguishes baseline verification from supplied checks. Live development attempt e4d1b9d9944e463ba1fbe15cea2883dc passed heading and record-count assertions in 64.42 seconds with zero repairs; run 687eadc3b182497d9a3114bef7173cdb records the evidence. All 195 Python tests passed, including rejection when baseline checks pass but request checks fail. Two desktop/mobile UI tests verified the completion wording. Arbitrary conversational requests still need a reviewed acceptance plan; these bounded assertions do not establish full semantic or design correctness.

Expected results are now editable in the conversational composer. Users can require visible headings, buttons, inputs, or an exact record count before sending an edit. Incomplete checks disable submission, rejected submissions keep the draft, and the conversation retains fixed checks after reload. Live UI-submitted attempt b050c668733542efa0765906865228c6 first passed baseline checks while missing the requested heading. Its fixed request check failed, returned the diagnostic to Bonsai, and the model repaired the heading in the same attempt. Run e1170fc339d942779ac82632e7ca8162 completed in 144.51 seconds with one repair. This is development evidence of the request repair path, not a general quality estimate. Automatic proposal of acceptance checks and richer behavioral assertions in the composer remain incomplete.

Excel XLSX intake now preserves nonempty cell values, sheet and cell addresses, formula text and cache status, including hidden sheets. Dates respect the workbook epoch, and expanded archives and sparse grids are bounded. Development workbook job 9626cb0ca71647b5be329b7766bbd695 completed as project dd2d5489a35c4dbcac513abc81f96a83; build run ab9dff4081f84a73b86f28d4044964bd passed source and interaction checks. The first planning response was correctly grounded but rejected because citation validation compared JSON false with Python False. Replaying that exact response with the corrected validator passed; the original failed validation and model repair remain preserved in planning run cb0bef05f4f84babae163b1067075732. The nine cells retain zero, false, an unverified cached formula value, a missing formula result, a stored error and a hidden-sheet note. Confirmation was development-only. WORKBOOK-VERIFICATION.json records the test scope and limits.

Supported text, table, Word and workbook uploads now save original bytes before extraction. Failures retain zero records, a visible error, a download link and an explicit retry action. Each retry preserves its previous manifest and result, with a separate MLflow run; an evidence service outage preserves the local outcome. A malformed development CSV verified both failed attempts and unchanged download bytes. Desktop and mobile checks passed, alongside the 204-test Python suite and an added evidence-outage regression. Svelte reported zero errors or warnings and the production build passed. RETAINED-INTAKE-VERIFICATION.json records the runs and limits. The real 79-page PDF proposal still awaits user confirmation. No human acceptance or model capability claim follows from this deterministic failure test.

The retained-intake backup attempt failed because MongoDB on 127.0.0.1:27028 refused the connection. Evidence exists locally and in MLflow, but these latest changes do not have a verified restore archive.

Structured proposal citations now link directly to their original source files and display source fields and available locations. PDF pages and audio/video timestamps use file fragments; workbook cells and Word body blocks are labeled alongside the original-file link. Missing source examples show an explicit unavailable message. Display-only replay of mixed-source job 60df4f14c15746fd9a34b2232c9abc8c passed desktop and mobile checks for unchanged quotes, original-file downloads and audio timestamps. No confirmation or inference was submitted. Svelte check, autofixer and production build passed. The proposal remains long and dense; source navigation improves review but does not establish full design quality or human acceptance.

Proposal revisions now preserve the exact parent proposal hash, parent planning run and archived source snapshot hash in the revision request and job status. Planning runs log their source snapshot hash and, for revisions, whether that snapshot changed from the parent. This distinguishes corrections under the same input from revisions after extraction or review changes. Revision feedback remains an unauthenticated interaction, not a training label. A regression verifies provenance capture without confirming or mutating the parent proposal. Historical runs are not backfilled with inferred lineage.

Proposal evidence previews now include the union of summary-finding citations and structured-record citations. Previously, a record cited only by the proposed structure could be absent from the preview. Historical job reads derive the corrected preview from the archived source packet without changing proposal contents, confirmation hashes or stored status. Seven source-job tests pass, including preservation of false values, deduplication, and exclusion of uncited records. Superseding older proposals after revision remains an open workflow issue discovered during this inspection.

A pending proposal is now exposed as superseded once a child revision has a validated proposal. Confirmation and further revision of that older version are rejected, while its archived status and proposal remain unchanged. A failed planning attempt without a validated replacement leaves the prior proposal available. Revision startup rechecks the parent under the model-lane lock to avoid acting on a version that changed between request validation and execution. Source-job tests cover rejected stale confirmation, failed-revision recovery and preservation of the original archive. This closes the stale-proposal issue noted above; it does not establish broader workflow reliability.

Live development job 8b56526fe4af47cc94547aee7f41de8f failed before proposal confirmation, so the intended end-to-end revision check did not execute. MLflow run fdb1e82b75f048218122eecde9f75235 preserves both responses: the first omitted required structured records, and the one allowed repair cited locator source_filename as a data field. The validator rejected that citation. REVISION-FLOW-FAILURE.json records the failure and a hypothesis to clarify eligible citation fields. No UI was built, no human label was added, and this does not count as revision-path success.

The user's conversation-first reference now guides the Workstreams shell. The navigation tab is renamed, a searchable conversation list exposes saved projects and pending source proposals, and the new-workstream screen uses a bottom message/attachment composer with secondary extraction controls collapsed. Existing views sit beside the edit conversation. Selecting a pending source restores its proposal; a fresh draft does not display unrelated proposal history. Four desktop/mobile checks passed for source selection, fresh drafts, opening a saved visualization and failed-upload recovery. Type checking reported zero errors or warnings and the production build passed. Screenshots 23 and 24 record the design. This is an interface organization change over existing source-job/project storage, not a new durable workstream entity; richer unified history and mobile navigation remain design opportunities. No real PDF confirmation or model call was submitted during verification.

Generated Workstreams now include their saved planning ancestry in the conversation: the original request, proposal corrections and each available interpretation, with MLflow evidence links. Source-job parent IDs determine the sequence, with cycle protection; no messages or acceptance labels are invented. The Data tab opens the source attached to the latest planning job. Desktop/mobile checks verified the displayed request and rationale against the saved workbook job, evidence-link identity, persistence after reopening and source selection. Type checking and production build passed. Screenshot 25 records the result. This joins existing persisted history in the interface; it is not a new workstream storage model.

The planning prompt now explicitly distinguishes data fields eligible for evidence quotes from locator metadata. Missing-field validation lists the actual eligible data keys. Replaying the unchanged failed response from run fdb1e82b75f048218122eecde9f75235 still rejects its source_filename citation, now with a diagnostic directing it to text. Fifteen source-job/workbook tests passed. CITATION-DIAGNOSTIC-REPLAY.json records the exact response replay and prompt hash. This verifies the diagnostic, not improved model success; a fresh model comparison remains required.

The clarified citation prompt produced a valid three-record proposal in job 78ffb737bb3f46a3b47fd65d4af75466 after one repair (run 9645cfeddc864210bdd489f467ecac3b). Source manifest, packet and recorded model configuration match the preceding failed experiment byte-for-byte; this remains a development comparison. Its revision job 60e5c6b2fe44425ba69f7bbcede0e1b7 also validated after one repair but ignored the explicitly requested title. The records and timestamps were retained. MLflow run d27a08c609ac48dda432c182c6db6600 records correct parent lineage and unchanged source snapshot. The parent became superseded and stale confirmation returned HTTP 409. LIVE-REVISION-VERIFICATION.json explicitly marks workflow success false because validation did not establish request adherence. No new proposal was confirmed or built. Backup verification remains unavailable due to the local MongoDB connection.

User feedback identified the Workstreams starting screen as a regression. The intake surface now restores progressive disclosure with Conversation, Files and Activity tabs. A single visible composer owns attachment and Send controls; drafts survive tab changes, and source records/review controls remain in Files. Roles (Research analyst, Data analyst, Evidence reviewer) are separated from shared attached-file search, and the selected role is included in planning requests. These are role instructions for the existing Bonsai harness, not separate autonomous agents or filesystem-wide search. Existing generated-view tabs remain intact. Desktop/mobile interaction checks uploaded a development table, switched tabs, asserted source details were hidden in Conversation, intercepted Send without inference, verified role/request payload and retained the rejected draft. Type check and build passed. Screenshots 26/27 record the repaired surface. Broader user acceptance and durable role metadata remain unverified.

The failed correction run exposed a context mismatch: the archived previous proposal used full source IDs while the current packet used short aliases. Revision input now remaps prior citation references into the current packet namespace and presents the prior proposal as an assistant message followed by the explicit user correction. Archived proposals stay unchanged. Sixteen targeted tests passed; replay of the historical revision confirms all prior citations map to shown IDs and feedback is the final user message. REVISION-CONTEXT-REPLAY.json records this input repair. Fresh model adherence is not yet verified, and exact-title compliance is not guaranteed by this change.

Live revision job 6e83f104314444c88a3cb5b93fe79240 applied the previously ignored exact title on its first response after the revision-context change. Planning run f26fef0f848240938e6a556f5b23ddad had zero repairs. All three structured records, citations and timestamps were identical to the parent. Development-only confirmation produced project 9181446da63f4da5a486f8ce094df90b; build run 13c13f7b88b64d208a9b596d90dc9763 completed source-aware checks. Additional desktop/mobile tests verified the heading, three records and preserved correction history. REVISION-CONTEXT-LIVE.json and screenshot 28 preserve the evidence. This is one development success, not a controlled reliability estimate or human acceptance. The real PDF remains unconfirmed. The backup attempt still failed against the unavailable local MongoDB connection.

Shared attached-file search now switches the current intake tab rather than starting a new conversation and discarding its draft. New workstreams reset to Conversation. Existing project edit drafts and expected-result checks are retained separately per project during the current page session; switching projects no longer carries an unrelated message into the destination. Four desktop/mobile checks verified repeated tool navigation, new-workstream reset, isolated project drafts, and the role composer submission path. Type check and build passed. Drafts are session memory only and do not survive a full page reload; no broader persistence claim is made.

Planning profiles now retain PDF extraction coverage instead of discarding the extraction_coverage field. Only per-page diagnostic entries are removed from the model-facing copy; page totals, text coverage, OCR pages, unresolved pages and limitations remain. The same compaction applies to collection members. Nineteen packet/job/collection tests passed, including a missing-page fixture and proof that the original diagnostic archive is unchanged. The saved 79-page PDF profile compacts from 9,809 to 1,486 JSON characters while retaining its text-only interpretation limitation. Its pending proposal was not regenerated or confirmed. This verifies context construction, not the model's interpretation of the limits.

Collection context sampling now alternates between attached sources while retaining question-relevance order within each source. The packet reports records shown per member and explicitly identifies unrepresented attachments; planning instructions require disclosure of those omissions. Tests demonstrate that a small attachment survives a budget otherwise dominated by a larger matching file, while an insufficient budget truthfully reports zero representation. The existing Word/audio snapshot retains all three records across both files. This improves bounded context selection, not exhaustive ingestion or semantic retrieval; a source whose records cannot fit may still be omitted with explicit coverage metadata.

Proposal review now displays per-attachment context counts when recorded member coverage is available, including a visible warning for files with zero included records. Aggregate wording now says included in model context rather than read, separating supplied input from verified understanding. A display-only omission scenario passed desktop/mobile checks and visual inspection; no historical packet or proposal was rewritten. Older runs without member coverage do not invent it. Svelte check, autofixer and build passed. The proxy was reloaded while idle to activate the previously tested balanced sampler for subsequent requests. Screenshot 29 records the omitted-file disclosure.

Confirmed builds now copy the reviewed planning-coverage summary into compiled project data. The maintained generated-view starter exposes it in a collapsed Source coverage section, including per-file counts and omission warnings when available. The confirmation-path regression verifies exact propagation from the pending proposal into the saved fixture. Nine source-job tests passed; the starter compiled with zero bundler or Svelte warnings and the autofixer reported no issues. Existing project revisions are unchanged. This verifies data propagation and compilation; this new starter section has not yet received rendered interaction review.

The generated-view coverage disclosure now has rendered interaction evidence. An isolated development store used the maintained starter and copied three-record fixture with explicitly synthetic omission metadata. Desktop/mobile checks verified the disclosure starts collapsed, opens the omitted-file warning, closes again, retains all three records and causes no document overflow. Screenshot 30 records visual inspection. The temporary preview server was closed in a finally block; live project files and proposals were not changed. This closes the starter interaction-review gap from the preceding entry, not broader data interpretation validation.

Intake drafts now survive page reload and component navigation in the same browser. Storage is scoped by selected role and initial source, and contains the question, source references and correction preference rather than copied file contents. Clear draft resets the local intake without deleting uploaded sources. Failed browser storage is disclosed and does not block composing. Eight desktop/mobile interaction checks passed, including question-only reload, attachment recovery, role isolation, clearing across reload and unavailable storage. Svelte check reported zero errors and warnings; the production build passed. Autofixer reported no issues; its effect suggestions were reviewed because persistence is an external side effect and the resulting notice is not an effect dependency. The refreshed mobile composer screenshot was inspected. Tests uploaded development JSON and intercepted generation; no model run or human review was created. This is one browser-local draft per role/source, not a synchronized durable conversation entity, and sending a request still requires extracted source data.

The generated-view starter now offers a collapsed date explorer for fields classified as dates. Inclusive date boundaries combine with node membership and text search. Unknown dates have an explicit inclusion control; reversed boundaries display an error and no matching records. Clear filters resets the range. Evidence notes retain their selected record, with a visible notice when it is outside the filtered records. The static Semiotic chart remains a full-dataset overview and is labeled accordingly when filters are active. This does not add a time brush or redraw the graph. Existing generated project revisions remain unchanged; subsequent builds use the updated starter.

The reproducible `research/harness-alignment/check_date_exploration.py` runner compiles a clearly synthetic five-record release fixture, renders a real Semiotic ForceDirectedGraph, builds the maintained Svelte starter and exercises an isolated preview. Desktop and mobile checks passed for a parameter-size node, same-day inclusive boundaries, missing dates, reversed bounds, note saving with distinct viewport-specific notes, selection context and clearing filters. Svelte autofixer reported no issues or suggestions. Screenshot 31 was inspected at mobile width; node labels in the full graph remain small on that viewport. The temporary preview and database are closed after verification. No live source, real release assertion, proposal confirmation, model run or human training label was created.

Generated charts now have bounded zoom controls, a keyboard-focusable scroll region and a fit reset. Fit uses the available width; zoom uses the renderer's native width from 100% through 300%. Node hit targets retain their coordinate alignment, and a selected node's full label appears below the chart. Four desktop/mobile checks passed across date exploration and zoom, including pointer selection at 150%, coordinate alignment within one pixel, retained membership after fit reset and no document overflow. The maintained starter compiled through the isolated WorkspaceTools build and Svelte autofixer reported no issues or suggestions. Screenshot 32 was inspected on mobile. The force layout still has some overlapping labels; zoom does not solve layout collisions or add dragging. The isolated preview was closed. Existing project revisions and live proposals were not modified.

Attempt comparison now reports mismatched task fields and missing or explicitly unverified configuration tags. Equal missing values no longer disappear solely because the configuration difference list is empty. Completeness means recorded metadata, not independent verification or a controlled experiment. Five comparator tests passed, including placeholder values and absent tags. A read-only comparison of the latest two mixed-source edit attempts found matching source data and model settings, but differing base revisions, requests, initial model context, request checks and harness revisions. CONFIGURATION-COMPARISON-AUDIT.json preserves that evidence; it cannot support a model-quality ranking.

The Workstreams Evidence tab exposes these details in a collapsed comparison disclosure. Svelte check reported zero errors/warnings and the production build passed. Two desktop/mobile checks used the historical report with an explicitly display-only missing-hardware scenario; the initial test referenced an incorrect project title, was stopped and corrected from the store, then passed. Screenshot 33 was inspected at mobile width. The idle proxy was reloaded and the actual comparison endpoint returned the new metadata. The real PDF remains awaiting confirmation. No inference or human review label was added.

Standalone interpretation curation now exists as an append-only proposal review API and collapsed control beside the proposal. Accept, reject and defer judgments are separate from build confirmation. Each judgment binds the proposal hash, source snapshot, exact question and model-visible packet; stale or superseded versions cannot supply accepted examples. Export includes only the latest human-declared acceptance and preserves the target, source manifest, packet, review and planning run/trace references. Completed jobs use their archived planning status rather than incorrectly linking the build trace. Changed source data, question or packet invalidates prior acceptance. Review identity remains self-declared. The bundle currently downloads locally; direct MLflow publication remains unfinished.

Seven new policy tests and the full 221-test Python suite passed. Svelte type checking reported no errors/warnings, the production build passed, and the new review/proposal components passed the autofixer. The build staging script now copies the new review component and the previously omitted expected-results component. Existing unrelated autofixer suggestions in WorkspaceData concern its reviewed persistence effect and media bindings. Two desktop/mobile UI checks inspected a completed development proposal without build controls, saved explicitly test-origin judgments and verified zero eligible human examples. A select locator initially timed out; selecting by its accessible combobox role corrected the test. Screenshot 34 was inspected. The live proxy was reloaded while idle. No human acceptance or inference was created, and the real PDF still awaits confirmation.

Interpretation review bundles can now be published to MLflow from the review control. The publisher saves a local bundle before network calls, records its dataset hash and eligible-example count, and retains the local artifact if publication fails. Eligible examples include verified original bytes and source/model/harness evidence without requiring a build confirmation. Interface exports retain their stricter build-evidence requirement. Thirteen targeted tests passed for review eligibility, publication failure, original integrity and independent interpretation export. The UI passed Svelte checking with zero errors/warnings, autofixer and production build.

Two live desktop/mobile browser checks published explicitly test-origin reviews. Both MLflow runs finished with zero eligible examples and training_executed=false. The saved artifacts were downloaded again and their dataset hashes and counts matched the API result and run metadata. PROPOSAL-PUBLICATION-VERIFICATION.json preserves the run links. Screenshot 34 records the publication result. This closes the direct-publication gap; it does not supply real human labels, validate a nonempty real curated dataset, or execute training. Positive source inclusion and tampering rejection were exercised only with isolated synthetic unit fixtures.

Saved workstreams now expose the complete source interpretation and its review/publication controls under Evidence, independent of interface review. The source passages, field structure and uncertainties remain inspectable in read-only proposal mode; completed projects do not show build approval controls. The browser test now follows the actual sidebar-to-Evidence route instead of rewriting the source-job listing to expose a completed upload. Both desktop and mobile checks passed and again published zero-example test datasets. Type checking and production build passed after correcting the guarded optional-value type. Screenshot 34 was inspected in the saved workstream. No model call or human judgment was made.

Controlled-edit preparation now supports a per-attempt profile and seed through the existing Workspace worker. An independent provider keeps endpoint, model, timeouts, byte bounds and prefix caching intact; the default provider is not mutated. Actual generation settings are saved with the attempt. Trial-copy preparation checks the source revision and shared edit/source-job guard, copies exact files and fixture into a fresh conversation and note namespace, and leaves the original project unchanged. The full 229-test Python suite passed, including independent provider settings, default reuse, source revision conflicts and equal trial starting revisions. No inference was performed in this preparation step.

CONTROLLED-EDIT-PROTOCOL.json freezes a mixed Word/audio filtering task and ten browser steps for fresh ABBA trials of legacy-greedy and bonsai2-instruct at seed 42. Each trial must match dataset, initial code, prompt, request, initial model input and check hashes. Shared warm cache and repeated development data preclude cold-latency or held-out claims. Search composition, exact evidence and notes still require supplementary checks. The protocol is prepared, not executed, and does not establish a preferred model configuration.

Conversation intake now has a live development check. Bonsai run 186fd3b69ca74e3d9cfabdaafc801ddd answered a question before attachment, and the reply survived reload. Source planning run e5472c9d3487445fb1b1dd8b4c377399 received that exact discussion as intent context, retained an unknown actual release date, and produced a source-linked proposal. No confirmation was submitted. INTAKE-CONVERSATION-VERIFICATION.json records both traces and the scope of the checks.

Saved workstream discovery is derived from persisted job lineage. A conversation appears under its role and original question, with later source jobs linked to the same entry. Desktop and mobile checks cleared browser storage and recovered the real development reply, CSV and pending proposal through that entry. Unit checks cover source revisions, failed follow-ups and malformed cyclic lineage. All 233 Python tests passed. This does not provide authenticated collaboration, cross-machine sync, durable unsent drafts or broad model-quality validation.

The frozen four-run ABBA development comparison has since completed. All four edits passed their frozen checks and supplementary source, search and note checks without repair. A later observational accessibility check found explicit selected-state semantics in both instruct-profile outputs and neither greedy-profile output. That later check was not a frozen success gate. CONTROLLED-EDIT-RESULTS.json preserves the distinction. These repeated development trials did not change the default profile or establish general model superiority.

The recovered conversation now scrolls in its own desktop pane above the composer. A browser geometry check verifies that the conversation pane ends before the composer begins, so long proposals can be read without that control covering them. Mobile uses the page scroll.

The request-check contract now supports explicit button selection through aria-pressed, with ordered clicks and selected or not-selected expectations in the composer. A post-run replay rejected the preserved greedy output that omitted this state and accepted the preserved instruct output on desktop and mobile. These are additional checks on old development outputs, not replacements for the frozen trial outcomes.

Bonsai then edited a fresh copy of the greedy output under nine fixed selection and record-count checks. Attempt f549a3dfe606466da23d1f200894a115 completed without repair retries. Its diff adds aria-pressed bindings to the three existing filter buttons. The original trial project was unchanged. The baseline source-fidelity checks, fixed desktop/mobile checks and supplementary search, source-link and note checks passed. This is one development repair, not a human acceptance or general model-quality estimate.

Video can now retain timestamped speech alongside sampled visual records. The speech pass uses the pinned English Whisper adapter on CPU and keeps the original video as the source. Independent speech and visual coverage enters planning. Either extraction order preserves the other channel, and a speech failure leaves an existing visual extraction usable. The file view labels speech and frames separately and seeks the original video to their timestamps.

A development video combined the existing cached observation table with synthetic team speech. It produced two speech segments and three visual records from one sampled frame. The UI passed desktop and mobile checks for channel labels, timestamp seeking and decoded-frame readiness. All 237 Python tests, Svelte checks and the production build passed. A test locator initially failed before selecting the source; using its accessible combobox role fixed the test. The full evidence is in VIDEO-SPEECH-VERIFICATION.json.

Planning run 976516187b784225b19882ae12759060 needed one repair for inconsistent record fields before returning a valid unconfirmed proposal. It preserved the channels and acknowledged that co-occurrence does not establish a link between them. Semantic review remains necessary: the model called repository observations release rows, and its table overview omitted the speech text even though full records retain it. No build confirmation or human review label was submitted. This is not evidence of native Bonsai audio reasoning or general video understanding. The backup attempt still failed because MongoDB refused the connection.
